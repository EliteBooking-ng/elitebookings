{
  "entities": {
    "ChatSession": {
      "title": "ChatSession",
      "description": "A chat session between a customer and the admin.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "customerEmail": { "type": "string" },
        "customerName": { "type": "string" },
        "lastMessage": { "type": "string" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["active", "closed"] }
      },
      "required": ["customerEmail", "customerName", "createdAt", "updatedAt", "status"]
    },
    "ChatMessage": {
      "title": "ChatMessage",
      "description": "A single message in a chat session.",
      "type": "object",
      "properties": {
        "sessionId": { "type": "string" },
        "senderId": { "type": "string" },
        "senderType": { "type": "string", "enum": ["customer", "admin"] },
        "text": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["sessionId", "senderType", "text", "timestamp"]
    },
    "Enquiry": {
      "title": "Enquiry",
      "description": "An automated booking enquiry from a customer.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "propertyName": { "type": "string" },
        "propertyLocation": { "type": "string" },
        "price": { "type": "string" },
        "checkin": { "type": "string" },
        "checkout": { "type": "string" },
        "clientPhone": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["propertyName", "propertyLocation", "checkin", "checkout", "clientPhone", "createdAt"]
    }
  },
  "firestore": {
    "/enquiries/{enquiryId}": {
      "schema": "Enquiry",
      "description": "Collection of automated booking enquiries."
    },
    "/chat_sessions/{sessionId}": {
      "schema": "ChatSession",
      "description": "Collection of chat sessions."
    },
    "/chat_sessions/{sessionId}/messages/{messageId}": {
      "schema": "ChatMessage",
      "description": "Subcollection of messages for a specific chat session."
    }
  }
}
